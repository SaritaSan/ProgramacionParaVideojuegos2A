using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* public class Accesos : MonoBehaviour
{
    public PersonajeBase personaje;
    public Heroe heroe;
    public EnemigoBase enemigo;

    // Start is called before the first frame update
    void Start()
    {
        if(PersonajeBase.isDead = false)
        {
            float combatResult = HeroeAgil;
        }
        if(!HeroeAgil.getHealth() >= 51)
        {
             
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/